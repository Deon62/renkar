{
  "name": "opa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve -s .",
    "build": "echo 'Build complete'",
    "start": "serve -s .",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
